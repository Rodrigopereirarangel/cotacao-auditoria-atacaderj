# Deploy da validade no app (colar na conversa do artifact)

**Para quem:** o Claude que roda dentro da conversa `claude.ai/chat/72d7582d`
(onde vive o artifact `cotacao-auditoria-atacaderj.html`).

**O que fazer:** aplicar as 4 mudanças abaixo no arquivo do artifact. O código é
idêntico ao do repositório (conferido byte a byte), então o patch encaixa exato.
**Não há selo de integridade** neste app (removido), então não precisa recalcular
hash nenhum — é só editar e salvar.

**O que essas mudanças fazem:** cada produto sugerido passa a mostrar até 2 datas
de validade (vindas do campo `vd` do `catalogo_bridge.json`); a data que vence em
menos de 45 dias ganha ⚠ e pode ser excluída pelo vendedor (só a marcada); o `.txt`
e o Excel do cliente ganham a validade + a nota "a mercadoria terá uma das duas — a
mais provável é a menor". Produto sem `vd` continua igual (nada aparece).

**Instrução ao Claude da conversa:** aplique este diff unificado ao
`cotacao-auditoria-atacaderj.html`. São 4 hunks; cada `-` sai e cada `+` entra,
usando as linhas de contexto (sem prefixo) para localizar. Depois salve o artifact.

```diff
diff --git a/app/cotacao-auditoria-atacaderj.html b/app/cotacao-auditoria-atacaderj.html
index 7f1a8e4..cec3e79 100644
--- a/app/cotacao-auditoria-atacaderj.html
+++ b/app/cotacao-auditoria-atacaderj.html
@@ -5013,6 +5013,16 @@ const ultimoMult=opcoes[opcoes.length-1].frac;if(max>1e-9&&max-ultimoMult>1e-9)o
 const _ajustePreco={};function _ajKey(item,cod){return item+'|#|'+cod;}
 function _aj(item,cod){const k=_ajKey(item,cod);return _ajustePreco[k]||(_ajustePreco[k]={desc:0,vMan:null,vuMan:null,aut:false});}
 function _precoEf(item,p){const a=_ajustePreco[_ajKey(item,p.c)]||{};const v=(a.vMan!=null)?a.vMan:(a.desc?Math.round(p.v*(1-a.desc)*100)/100:p.v);const vu=(a.vuMan!=null)?a.vuMan:p.vu;return{v,vu,desc:a.desc||0,aut:!!a.aut,editado:(a.vMan!=null||a.vuMan!=null)};}
+/* ---- VALIDADE: as 2 ultimas notas de entrada (chega no catalogo_bridge como p.vd=[iso,iso])
+   Consulta por CODIGO (nao viaja nas projecoes de produto) — mesmo padrao do _ajustePreco.
+   Produto sem validade registrada: _vals() volta [] e NADA e exibido/impresso. ---- */
+const VALIDADE_ALERTA_DIAS=45;const _valEx={};let _valIdx=null,_valIdxLen=-1;
+function _valMapa(){const arr=(typeof CATALOG!=='undefined'&&CATALOG)||[];if(_valIdx&&_valIdxLen===arr.length)return _valIdx;_valIdx={};for(const p of arr)if(p&&Array.isArray(p.vd)&&p.vd.length)_valIdx[p.c]=p.vd;_valIdxLen=arr.length;return _valIdx;}
+function _valDias(iso){const d=new Date(String(iso).slice(0,10)+'T00:00:00');if(isNaN(d))return null;const h=new Date();h.setHours(0,0,0,0);return Math.round((d-h)/86400000);}
+function _valFmt(iso){const p=String(iso).slice(0,10).split('-');return p.length===3?p[2]+'/'+p[1]+'/'+p[0]:String(iso);}
+function _vals(item,p){const todas=_valMapa()[p.c]||[];const ex=_valEx[_ajKey(item,p.c)];return todas.map((iso,i)=>{const dias=_valDias(iso);return{iso,i,dias,alerta:(dias!=null&&dias<VALIDADE_ALERTA_DIAS),fora:!!(ex&&ex.has(i))};});}
+function _valsVivas(item,p){return _vals(item,p).filter(v=>!v.fora);}
+function _valToggle(item,cod,i){const k=_ajKey(item,cod);const s=_valEx[k]||(_valEx[k]=new Set());if(s.has(i))s.delete(i);else s.add(i);}
 function _parsePtBr(s){s=String(s).trim().replace(/[^\d.,-]/g,'');if(s.includes(','))s=s.replace(/\./g,'').replace(',','.');return parseFloat(s);}
 function _fmtNumIn(n){return(Math.round(n*100)/100).toFixed(2).replace('.',',');}
 function _precoCellHtml(item,p){const ef=_precoEf(item,p);const un='';let tag='';if(ef.aut)tag=` <span class="badge-aut" title="preço abaixo do limite, autorizado pelo gerente">✔ gerente</span>`;else if(ef.desc>0)tag=` <span class="price-desc">−${_fmtPct(ef.desc)}</span>`;else if(ef.editado)tag=` <span class="price-desc">editado</span>`;return un+_fmtR(ef.v)+tag;}
@@ -5202,7 +5212,7 @@ if(!nhArq.ok)throw new Error(`"${file.name}" é de uma janela ANTIGA (gerado às
 const produtos=[];for(const r of obj.produtos){const c=parseInt(r.c);const p=String(r.p||'').toUpperCase().trim();const v=Number(r.v);let q=parseInt(r.q);if(!Number.isFinite(q)||q<1)q=1;
 if(!Number.isInteger(c)||p.length<4||!(v>0))continue;if(/MORTO|EXCLUIDO|<<<.*>>>/.test(p))continue;
 const item={c,p,q,v:Math.round(v*100)/100};const vu=Number(r.vu);if(vu>0&&Math.round(vu*100)/100!==item.v)item.vu=Math.round(vu*100)/100;
-const custo=Number(r.custo);if(custo>0)item.custo=Math.round(custo*100)/100;if(r.cv)item.cv=String(r.cv).trim().toUpperCase()[0];
+const custo=Number(r.custo);if(custo>0)item.custo=Math.round(custo*100)/100;if(r.cv)item.cv=String(r.cv).trim().toUpperCase()[0];if(Array.isArray(r.vd)){const _vd=r.vd.map(x=>String(x||'').slice(0,10)).filter(x=>/^\d{4}-\d{2}-\d{2}$/.test(x)).slice(0,2);if(_vd.length)item.vd=_vd;}
 produtos.push(item);}
 if(produtos.length<=50)throw new Error(`"${file.name}": só ${produtos.length} produtos válidos — arquivo suspeito de estar incompleto. Nada foi alterado no banco atual.`);
 if(Number.isInteger(obj.total)&&obj.total!==produtos.length)throw new Error(`"${file.name}": o arquivo declara ${obj.total} produtos, mas a leitura validou ${produtos.length}. Gere de novo pelo bridge — nada foi alterado no banco atual.`);
@@ -5505,7 +5515,10 @@ function _revRowHtml(ii,gi,pi,p,item,sid,cb){const ex=p._ex;const ef=_precoEf(it
       <span style="color:${ex?'var(--text-faint)':'var(--text)'};${ex?'text-decoration:line-through':''};font-weight:600">${_escHtml(p.p)}${p._man?' <span style="color:var(--green);font-weight:700;font-size:10px">novo</span>':''}${(!ex && p.c===cb)?' <span class="tag-barato" title="Menor preço desta subcategoria">↓ + barato</span>':''}</span>
       <span style="text-align:right;font-variant-numeric:tabular-nums">${precoHtml}</span>
       <button onclick="revToggle(${ii},${gi},${pi})" title="${ex?'Desfazer exclusão':'Excluir da cotação'}" style="width:24px;height:24px;border:none;border-radius:6px;cursor:pointer;font-size:11px;background:${ex?'var(--green-bg)':'var(--red-bg)'};color:${ex?'var(--green)':'var(--red)'}">${ex?'↺':'✕'}</button>
-    </div>`;html+=ex?`<div style="border-bottom:1px solid var(--line-soft)"></div>`:`<div class="item-ctrl" style="padding:2px 0 9px;background:transparent;border-bottom:1px solid var(--line-soft)">${_ctrlHtml(sid, item, p)}</div>`;html+=`</div>`;return html;}
+    </div>`;html+=ex?`<div style="border-bottom:1px solid var(--line-soft)"></div>`:`${_valChipsHtml(ii,gi,pi,item,p)}<div class="item-ctrl" style="padding:2px 0 9px;background:transparent;border-bottom:1px solid var(--line-soft)">${_ctrlHtml(sid, item, p)}</div>`;html+=`</div>`;return html;}
+/* chips de validade na revisao: verde = ok; laranja+⚠ = vence em <45 dias (e so essa pode ser excluida) */
+function _valChipsHtml(ii,gi,pi,item,p){const vs=_vals(item,p);if(!vs.length)return'';const chips=vs.map(v=>{if(v.fora)return`<span style="display:inline-flex;align-items:center;gap:4px;font-size:10.5px;color:var(--text-faint)"><s>${_valFmt(v.iso)}</s> removida<button onclick="revValToggle(${ii},${gi},${pi},${v.i})" style="border:1px solid var(--info-bd);background:var(--info-bg);color:var(--accent);border-radius:6px;font-size:10px;font-weight:700;cursor:pointer;padding:2px 6px">↺ desfazer</button></span>`;const al=v.alerta;const x=al?`<button onclick="revValToggle(${ii},${gi},${pi},${v.i})" title="Excluir esta validade da cotação" aria-label="Excluir validade ${_valFmt(v.iso)}" style="border:none;background:transparent;color:inherit;cursor:pointer;font-weight:900;font-size:11px;line-height:1;padding:0 0 0 2px">✕</button>`:'';return`<span style="display:inline-flex;align-items:center;gap:5px;font-size:11px;font-weight:700;border-radius:999px;padding:3px 8px;border:1px solid ${al?'var(--orange-bd)':'var(--green-bd)'};background:${al?'var(--orange-bg)':'var(--green-bg)'};color:${al?'var(--orange-ink)':'var(--green)'}">${al?'⚠ ':''}${_valFmt(v.iso)}${v.dias!=null?`<span style="font-weight:600;opacity:.8;font-size:10px">(${v.dias}d)</span>`:''}${x}</span>`;}).join('');return`<div style="display:flex;flex-wrap:wrap;gap:6px;align-items:center;padding:3px 0 5px 60px"><span style="font-size:10px;font-weight:800;letter-spacing:.4px;text-transform:uppercase;color:var(--text-faint)">Validade</span>${chips}</div>`;}
+function revValToggle(ii,gi,pi,vi){if(!_revData)return;const d=_revData[ii];const p=d.grupos[gi]&&d.grupos[gi].produtos[pi];if(!p)return;_valToggle(d.item,p.c,vi);const bloco=document.getElementById('revItem_'+ii);if(bloco)bloco.outerHTML=_revItemHtml(d,ii);}
 function _revItemHtml(d,ii){const nProd=d.grupos.reduce((s,g)=>s+g.produtos.length,0);const sid=d.item.replace(/[^a-zA-Z0-9]/g,'_');_cardKeyBySid[sid]=d.item;let html=`<div id="revItem_${ii}" style="padding:8px 0 12px;border-bottom:2px solid var(--line)">
     <div style="font-size:13px;font-weight:800;color:var(--navy);margin-bottom:4px">${_escHtml(d.item.toUpperCase())} ${d.erro?'<span style="color:var(--red);font-weight:700;font-size:11px">⚠ erro na busca — adicione manualmente se quiser cotar</span>':(nProd?'':'<span style="color:var(--text-faint);font-weight:600;font-size:11px">— nenhum produto encontrado</span>')}</div>`;d.grupos.forEach((g,gi)=>{if(d.grupos.length>1){const todosEx=g.produtos.length>0&&g.produtos.every(p=>p._ex);html+=`<div style="display:flex;align-items:center;justify-content:space-between;gap:8px;margin:10px 0 3px;padding:7px 10px 7px 9px;background:var(--info-bg);border-left:4px solid var(--orange)">
         <span style="font-size:11.5px;font-weight:800;color:var(--navy);text-transform:uppercase;letter-spacing:.5px">${_escHtml(g.nome||'Produtos')}</span>
@@ -5533,16 +5546,20 @@ function revToggleGrupo(ii,gi){if(!_revData)return;const g=_revData[ii].grupos[g
 function atualizarBotaoRev(){const btn=document.getElementById('revGerar');if(!btn||!_revData)return;const n=_revData.reduce((s,d)=>s+d.grupos.reduce((s2,g)=>s2+g.produtos.filter(p=>!p._ex).length,0),0);btn.textContent=`✓ Salvar cotação (${n} produto${n!==1?'s':''})`;}
 function confirmarRevisao(){if(!_revData)return;const _candIA=[];let _nCorrigidos=0;for(const d of _revData){const _ed=d.grupos.some(g=>(g.produtos||[]).some(p=>p._man||p._ex));if(_ed)_nCorrigidos++;const _aprender=d.grupos.flatMap(g=>g.produtos).filter(p=>p._man&&!p._ex).map(p=>p.c);if(_aprender.length)aprenderApelido(d.item,_aprender);d.grupos.flatMap(g=>g.produtos).filter(p=>!p._ex&&!p._man).forEach(p=>{if(!(_apelidos.get(chaveBusca(d.item))||[]).includes(p.c))_candIA.push({termo:d.item,cod:p.c,nome:p.p});});const grupos=d.grupos.map(g=>({nome:g.nome,produtos:g.produtos.filter(p=>!p._ex).map(p=>({c:p.c,p:p.p,q:p.q,v:p.v,vu:p.vu,custo:p.custo,cv:p.cv}))})).filter(g=>g.produtos.length>0);const mudou=JSON.stringify(searchResults[d.item]||[])!==JSON.stringify(grupos);if(d.erro&&grupos.length)cotacaoErros.delete(d.item);if(mudou)renderBody(d.item,grupos);}
 _aprenderApelidosIA(_candIA);try{_metricaAdd({corrigidos:_nCorrigidos,itens_revisados:(_revData?_revData.length:0)});}catch(e){}_fecharRevisaoInline();_revData=null;gerarTxtFinal();}
-function gerarTxtFinal(){const _agora=new Date();const hoje=_agora.toLocaleDateString('pt-BR');const hora=_agora.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'});const W=30;const centro=s=>{const t=String(s).trim();const pad=Math.max(0,Math.floor((W-t.length)/2));return' '.repeat(pad)+t;};const subBar=s=>{const t=' '+String(s).toUpperCase().trim()+' ';const f=Math.max(2,Math.floor((W-t.length)/2));return'─'.repeat(f)+t+'─'.repeat(Math.max(2,W-t.length-f));};const lines=['='.repeat(W),centro('ATACADERJ'),centro('COTAÇÃO DE PREÇOS'),'='.repeat(W)];if(clienteNome)lines.push('Cliente: '+clienteNome);lines.push('Gerada '+hoje+' '+hora+' · válida só hoje');const listaItens=cotacaoItensOrdem.length?cotacaoItensOrdem:Object.keys(searchResults);let num=0,nCotados=0,nProdutos=0;for(const item of listaItens){if(cotacaoErros.has(item))continue;const grupos=searchResults[item];const gruposOk=(grupos||[]).filter(g=>g&&Array.isArray(g.produtos)&&g.produtos.length>0);if(gruposOk.length===0)continue;num++;lines.push('');lines.push('='.repeat(W));lines.push(centro(num+') '+item.toUpperCase()));lines.push('='.repeat(W));nCotados++;for(const g of gruposOk){if(gruposOk.length>1){lines.push('');lines.push(subBar(g.nome||'PRODUTOS'));}
-const cb=_codBarato(g.produtos);for(const p of g.produtos){if(!p||!p.p||typeof p.v!=='number'||p.q==null)continue;nProdutos++;const ef=_precoEf(item,p);const obs=ef.aut?'  [preço autorizado pelo gerente]':(ef.desc>0?'  ('+_fmtPct(ef.desc)+' desc.)':'');lines.push('• '+p.p+' (cód '+p.c+')'+(p.c===cb?'  ⬇ + barato':''));if(_temVarejo(p)){lines.push('   un: '+_fmtR(ef.vu));lines.push('   atacado (mín '+p.q+'): '+_fmtR(ef.v)+obs);}else if(p.q>1){lines.push('   atacado (mín '+p.q+'): '+_fmtR(ef.v)+obs);}else{lines.push('   un: '+_fmtR(ef.v)+obs);}}}}
-lines.push('');lines.push('='.repeat(W));lines.push(centro(nCotados+(nCotados===1?' item':' itens')+' · '+nProdutos+(nProdutos===1?' preço':' preços')));lines.push(centro('@atacaderjceasa'));lines.push('='.repeat(W));baixarTxt(lines.join('\n'),`cotacao_${nomeArquivoCliente()}_${timestampArquivo()}.txt`,true);}
+function gerarTxtFinal(){const _agora=new Date();const hoje=_agora.toLocaleDateString('pt-BR');const hora=_agora.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'});const W=30;const centro=s=>{const t=String(s).trim();const pad=Math.max(0,Math.floor((W-t.length)/2));return' '.repeat(pad)+t;};const subBar=s=>{const t=' '+String(s).toUpperCase().trim()+' ';const f=Math.max(2,Math.floor((W-t.length)/2));return'─'.repeat(f)+t+'─'.repeat(Math.max(2,W-t.length-f));};const lines=['='.repeat(W),centro('ATACADERJ'),centro('COTAÇÃO DE PREÇOS'),'='.repeat(W)];if(clienteNome)lines.push('Cliente: '+clienteNome);lines.push('Gerada '+hoje+' '+hora+' · válida só hoje');const listaItens=cotacaoItensOrdem.length?cotacaoItensOrdem:Object.keys(searchResults);let num=0,nCotados=0,nProdutos=0,_temValidade=false;for(const item of listaItens){if(cotacaoErros.has(item))continue;const grupos=searchResults[item];const gruposOk=(grupos||[]).filter(g=>g&&Array.isArray(g.produtos)&&g.produtos.length>0);if(gruposOk.length===0)continue;num++;lines.push('');lines.push('='.repeat(W));lines.push(centro(num+') '+item.toUpperCase()));lines.push('='.repeat(W));nCotados++;for(const g of gruposOk){if(gruposOk.length>1){lines.push('');lines.push(subBar(g.nome||'PRODUTOS'));}
+const cb=_codBarato(g.produtos);for(const p of g.produtos){if(!p||!p.p||typeof p.v!=='number'||p.q==null)continue;nProdutos++;const ef=_precoEf(item,p);const obs=ef.aut?'  [preço autorizado pelo gerente]':(ef.desc>0?'  ('+_fmtPct(ef.desc)+' desc.)':'');lines.push('• '+p.p+' (cód '+p.c+')'+(p.c===cb?'  ⬇ + barato':''));if(_temVarejo(p)){lines.push('   un: '+_fmtR(ef.vu));lines.push('   atacado (mín '+p.q+'): '+_fmtR(ef.v)+obs);}else if(p.q>1){lines.push('   atacado (mín '+p.q+'): '+_fmtR(ef.v)+obs);}else{lines.push('   un: '+_fmtR(ef.v)+obs);}const _vv=_valsVivas(item,p);if(_vv.length){_temValidade=true;lines.push('   validade: '+_vv.map(v=>(v.alerta?'⚠ ':'')+_valFmt(v.iso)).join('  ·  '));}}}}
+lines.push('');lines.push('='.repeat(W));if(_temValidade){lines.push('* A data de validade da mercadoria');lines.push('  será UMA das duas informadas —');lines.push('  a mais provável é a MENOR.');lines.push('  ⚠ = vence em menos de '+VALIDADE_ALERTA_DIAS+' dias.');lines.push('='.repeat(W));}
+lines.push(centro(nCotados+(nCotados===1?' item':' itens')+' · '+nProdutos+(nProdutos===1?' preço':' preços')));lines.push(centro('@atacaderjceasa'));lines.push('='.repeat(W));baixarTxt(lines.join('\n'),`cotacao_${nomeArquivoCliente()}_${timestampArquivo()}.txt`,true);}
 function nomeArquivoCliente(){const slug=clienteNome.normalize('NFD').replace(/[̀-ͯ]/g,'').replace(/[^a-zA-Z0-9 ]/g,'').trim().replace(/\s+/g,'_');return slug||'atacaderj';}
 function timestampArquivo(){const d=new Date(),p=n=>String(n).padStart(2,'0');return`${p(d.getDate())}-${p(d.getMonth()+1)}-${d.getFullYear()}_${p(d.getHours())}h${p(d.getMinutes())}`;}
 let _txtConteudo='',_txtNome='';function exportarCotacaoExcel(btn){if(typeof XLSX==='undefined'||!XLSX.utils){alert('A biblioteca de Excel não carregou. Recarregue o app e tente de novo.');return;}
-const hoje=new Date().toLocaleDateString('pt-BR');const listaItens=cotacaoItensOrdem.length?cotacaoItensOrdem:Object.keys(searchResults);const linhas=[];let num=0;for(const item of listaItens){if(cotacaoErros.has(item))continue;const grupos=(searchResults[item]||[]).filter(g=>g&&Array.isArray(g.produtos)&&g.produtos.length>0);if(!grupos.length)continue;num++;let primeira=true;for(const g of grupos){if(grupos.length>1){linhas.push({cells:[primeira?(num+') '+item):'','▸ '+(g.nome||'PRODUTOS').toUpperCase(),'','','',''],tipo:'grupo',primeira,itemIdx:num});primeira=false;}
-const cb=_codBarato(g.produtos);for(const p of g.produtos){if(!p||!p.p||typeof p.v!=='number'||p.q==null)continue;const ef=_precoEf(item,p);const temVu=_temVarejo(p);const un=temVu?ef.vu:(p.q===1?ef.v:'');const qm=(temVu||p.q>1)?p.q:'';const at=(temVu||p.q>1)?ef.v:'';const barato=p.c===cb?'  ⬇ + barato':'';const obs=ef.aut?'  [autorizado pelo gerente]':(ef.desc>0?'  (−'+_fmtPct(ef.desc)+' desc.)':'');linhas.push({cells:[primeira?(num+') '+item):'',p.p+barato+obs,un,qm,at,p.c],tipo:'data',primeira,itemIdx:num});primeira=false;}}}
-const aoa=[['ATACADERJ — COTAÇÃO DE PREÇOS','','','','',''],['Cliente: '+(clienteNome||'—'),'','','Preços válidos hoje: '+hoje,'',''],['','','','','',''],['Item','Produto','Preço un (R$)','Qtd. mín','Preço atacado (R$)','Cód.']];linhas.forEach(l=>aoa.push(l.cells));const ws=XLSX.utils.aoa_to_sheet(aoa);const cel=(r,c)=>{const ref=XLSX.utils.encode_cell({r,c});if(!ws[ref])ws[ref]={t:'s',v:''};return ws[ref];};const F='Calibri';const bd={style:'thin',color:{rgb:'D6DEE8'}};const borda={top:bd,bottom:bd,left:bd,right:bd};const moeda='"R$" #,##0.00';const HR=3;cel(0,0).s={font:{name:F,bold:true,sz:15,color:{rgb:'FFFFFF'}},fill:{patternType:'solid',fgColor:{rgb:'0D3364'}},alignment:{horizontal:'center',vertical:'center'}};cel(1,0).s={font:{name:F,bold:true,sz:10,color:{rgb:'0D3364'}},alignment:{vertical:'center'}};cel(1,3).s={font:{name:F,sz:10,color:{rgb:'5C6B7A'}},alignment:{horizontal:'right',vertical:'center'}};for(let c=0;c<6;c++)cel(HR,c).s={font:{name:F,bold:true,sz:11,color:{rgb:'FFFFFF'}},fill:{patternType:'solid',fgColor:{rgb:'1E5DA0'}},alignment:{horizontal:c===1?'left':'center',vertical:'center',wrapText:true},border:borda};linhas.forEach((l,i)=>{const r=HR+1+i;const zebra=(l.itemIdx%2===0);for(let c=0;c<6;c++){const cell=cel(r,c);const al=(c===0||c===1)?'left':((c===3||c===5)?'center':'right');const st={font:{name:F,sz:10,color:{rgb:'1D2733'}},alignment:{horizontal:al,vertical:'center'},border:borda};if(zebra)st.fill={patternType:'solid',fgColor:{rgb:'F4F8FD'}};if(l.tipo==='vazio'&&c===1)st.font={name:F,sz:10,italic:true,color:{rgb:'94A1B2'}};if(c===0&&l.primeira)st.font={name:F,sz:10,bold:true,color:{rgb:'0D3364'}};if(l.tipo==='grupo'){st.font={name:F,sz:10,bold:true,color:{rgb:'0D3364'}};st.fill={patternType:'solid',fgColor:{rgb:'EAF1FB'}};}
-cell.s=st;if((c===2||c===4)&&typeof cell.v==='number')cell.z=moeda;}});ws['!merges']=[{s:{r:0,c:0},e:{r:0,c:5}},{s:{r:1,c:0},e:{r:1,c:2}},{s:{r:1,c:3},e:{r:1,c:5}}];ws['!cols']=[{wch:16},{wch:32},{wch:13},{wch:9},{wch:15},{wch:9}];ws['!rows']=[{hpt:28},{hpt:18},{hpt:6},{hpt:22}];ws['!freeze']={xSplit:0,ySplit:HR+1,topLeftCell:XLSX.utils.encode_cell({r:HR+1,c:0}),activePane:'bottomLeft',state:'frozen'};const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,ws,'Cotação');const nome=`cotacao_${nomeArquivoCliente()}_${timestampArquivo()}.xlsx`;try{const out=XLSX.write(wb,{bookType:'xlsx',type:'array'});const blob=new Blob([out],{type:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=nome;a.rel='noopener';a.style.display='none';document.body.appendChild(a);a.click();setTimeout(()=>{a.remove();URL.revokeObjectURL(url);},1500);if(btn){const t=btn.textContent;btn.textContent='✓ Excel baixado!';setTimeout(()=>{btn.textContent=t;},2500);}}catch(e){console.error('Excel:',e);alert('Não consegui gerar o Excel: '+e.message);}}
+const hoje=new Date().toLocaleDateString('pt-BR');const listaItens=cotacaoItensOrdem.length?cotacaoItensOrdem:Object.keys(searchResults);const linhas=[];let num=0,_temValidadeXls=false;for(const item of listaItens){if(cotacaoErros.has(item))continue;const grupos=(searchResults[item]||[]).filter(g=>g&&Array.isArray(g.produtos)&&g.produtos.length>0);if(!grupos.length)continue;num++;let primeira=true;for(const g of grupos){if(grupos.length>1){linhas.push({cells:[primeira?(num+') '+item):'','▸ '+(g.nome||'PRODUTOS').toUpperCase(),'','','','','',''],tipo:'grupo',primeira,itemIdx:num});primeira=false;}
+const cb=_codBarato(g.produtos);for(const p of g.produtos){if(!p||!p.p||typeof p.v!=='number'||p.q==null)continue;const ef=_precoEf(item,p);const temVu=_temVarejo(p);const un=temVu?ef.vu:(p.q===1?ef.v:'');const qm=(temVu||p.q>1)?p.q:'';const at=(temVu||p.q>1)?ef.v:'';const barato=p.c===cb?'  ⬇ + barato':'';const obs=ef.aut?'  [autorizado pelo gerente]':(ef.desc>0?'  (−'+_fmtPct(ef.desc)+' desc.)':'');const _vv=_valsVivas(item,p);if(_vv.length)_temValidadeXls=true;const _vC=v=>v?((v.alerta?'⚠ ':'')+_valFmt(v.iso)):'';linhas.push({cells:[primeira?(num+') '+item):'',p.p+barato+obs,un,qm,at,_vC(_vv[0]),_vC(_vv[1]),p.c],tipo:'data',primeira,itemIdx:num,vAl:[!!(_vv[0]&&_vv[0].alerta),!!(_vv[1]&&_vv[1].alerta)]});primeira=false;}}}
+const aoa=[['ATACADERJ — COTAÇÃO DE PREÇOS','','','','','','',''],['Cliente: '+(clienteNome||'—'),'','','','Preços válidos hoje: '+hoje,'','',''],['','','','','','','',''],['Item','Produto','Preço un (R$)','Qtd. mín','Preço atacado (R$)','Validade 1','Validade 2','Cód.']];linhas.forEach(l=>aoa.push(l.cells));let _notaRow=-1;if(_temValidadeXls){aoa.push(['','','','','','','','']);aoa.push(['* A data de validade da mercadoria será UMA das duas informadas — a mais provável é a MENOR.    ⚠ = vence em menos de '+VALIDADE_ALERTA_DIAS+' dias.','','','','','','','']);_notaRow=aoa.length-1;}
+const ws=XLSX.utils.aoa_to_sheet(aoa);const cel=(r,c)=>{const ref=XLSX.utils.encode_cell({r,c});if(!ws[ref])ws[ref]={t:'s',v:''};return ws[ref];};const F='Calibri';const bd={style:'thin',color:{rgb:'D6DEE8'}};const borda={top:bd,bottom:bd,left:bd,right:bd};const moeda='"R$" #,##0.00';const HR=3;cel(0,0).s={font:{name:F,bold:true,sz:15,color:{rgb:'FFFFFF'}},fill:{patternType:'solid',fgColor:{rgb:'0D3364'}},alignment:{horizontal:'center',vertical:'center'}};cel(1,0).s={font:{name:F,bold:true,sz:10,color:{rgb:'0D3364'}},alignment:{vertical:'center'}};cel(1,4).s={font:{name:F,sz:10,color:{rgb:'5C6B7A'}},alignment:{horizontal:'right',vertical:'center'}};for(let c=0;c<8;c++)cel(HR,c).s={font:{name:F,bold:true,sz:11,color:{rgb:'FFFFFF'}},fill:{patternType:'solid',fgColor:{rgb:'1E5DA0'}},alignment:{horizontal:c===1?'left':'center',vertical:'center',wrapText:true},border:borda};linhas.forEach((l,i)=>{const r=HR+1+i;const zebra=(l.itemIdx%2===0);for(let c=0;c<8;c++){const cell=cel(r,c);const al=(c===0||c===1)?'left':((c===3||c===5||c===6||c===7)?'center':'right');const st={font:{name:F,sz:10,color:{rgb:'1D2733'}},alignment:{horizontal:al,vertical:'center'},border:borda};if(zebra)st.fill={patternType:'solid',fgColor:{rgb:'F4F8FD'}};if(l.tipo==='vazio'&&c===1)st.font={name:F,sz:10,italic:true,color:{rgb:'94A1B2'}};if(c===0&&l.primeira)st.font={name:F,sz:10,bold:true,color:{rgb:'0D3364'}};if(l.tipo==='grupo'){st.font={name:F,sz:10,bold:true,color:{rgb:'0D3364'}};st.fill={patternType:'solid',fgColor:{rgb:'EAF1FB'}};}
+if((c===5||c===6)&&l.vAl&&l.vAl[c-5]){st.font={name:F,sz:10,bold:true,color:{rgb:'9A5A00'}};st.fill={patternType:'solid',fgColor:{rgb:'FDEED7'}};}
+cell.s=st;if((c===2||c===4)&&typeof cell.v==='number')cell.z=moeda;}});ws['!merges']=[{s:{r:0,c:0},e:{r:0,c:7}},{s:{r:1,c:0},e:{r:1,c:3}},{s:{r:1,c:4},e:{r:1,c:7}}];if(_notaRow>=0){ws['!merges'].push({s:{r:_notaRow,c:0},e:{r:_notaRow,c:7}});cel(_notaRow,0).s={font:{name:F,sz:9.5,italic:true,color:{rgb:'9A5A00'}},fill:{patternType:'solid',fgColor:{rgb:'FDEED7'}},alignment:{horizontal:'left',vertical:'center',wrapText:true},border:borda};}
+ws['!cols']=[{wch:16},{wch:32},{wch:13},{wch:9},{wch:15},{wch:12},{wch:12},{wch:9}];ws['!rows']=[{hpt:28},{hpt:18},{hpt:6},{hpt:22}];ws['!freeze']={xSplit:0,ySplit:HR+1,topLeftCell:XLSX.utils.encode_cell({r:HR+1,c:0}),activePane:'bottomLeft',state:'frozen'};const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,ws,'Cotação');const nome=`cotacao_${nomeArquivoCliente()}_${timestampArquivo()}.xlsx`;try{const out=XLSX.write(wb,{bookType:'xlsx',type:'array'});const blob=new Blob([out],{type:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=nome;a.rel='noopener';a.style.display='none';document.body.appendChild(a);a.click();setTimeout(()=>{a.remove();URL.revokeObjectURL(url);},1500);if(btn){const t=btn.textContent;btn.textContent='✓ Excel baixado!';setTimeout(()=>{btn.textContent=t;},2500);}}catch(e){console.error('Excel:',e);alert('Não consegui gerar o Excel: '+e.message);}}
 function baixarTxt(conteudo,nome,comExcel){_txtConteudo=conteudo;_txtNome=nome;let ov=document.getElementById('txt-overlay');if(ov)ov.remove();ov=document.createElement('div');ov.id='txt-overlay';ov.style.cssText='position:fixed;inset:0;background:rgba(0,0,0,.45);z-index:1001;display:flex;align-items:center;justify-content:center;padding:1rem';ov.onclick=e=>{if(e.target===ov)ov.remove();};ov.innerHTML=`
     <div class="order-modal" style="background:#fff;border-radius:14px;max-width:680px;width:100%;max-height:88vh;display:flex;flex-direction:column;padding:1.25rem">
       <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:6px;gap:8px">
```
